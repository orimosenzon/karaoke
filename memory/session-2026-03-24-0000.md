# Session 2026-03-24

## What We Did
- Fixed Wikipedia disambiguation bug: `fetch_wikipedia_summary` now retries with fallback queries when result type is "disambiguation" (e.g. adds "song" / "singer" suffixes)
- Fixed `|` suffix in YouTube titles (e.g. "חלק מהזמן | Idan Amedi") — now stripped before search
- Added cache invalidation for songs stored as `source: "none"` — they are re-fetched on next load
- Added UX hints during loading: `STAGE_LABELS` with `hint` field, `STAGE_HINTS_TIMED` for time-based escalation (cold start hint after 8s), `_startHintTimers()` / `_clearHintTimers()`
- Explored Shironet (shironet.mako.co.il) as Hebrew lyrics source using Playwright:
  - Got it working locally (21s, correct lyrics)
  - Decision: removed due to Docker image bloat (~300MB for Chromium → longer Railway cold starts)
- Fixed Hebrew captions: `_try_youtube_captions` now requests `["he", "en", "en-orig", "es", "fr", ...]` — Israeli songs on YouTube have Hebrew auto-captions that were never fetched

## Decisions
- No Playwright/Chromium in Docker image — cold start impact not worth it
- Hebrew captions from YouTube is the preferred free zero-dependency solution for Israeli songs
- `synced: source != "lrclib_plain"` — tracks whether lyrics have timing

## Next Steps
- Test "חלק מהזמן" by עידן עמדי after Railway deploy — should now get Hebrew captions
- Consider adding more lyrics APIs that support Hebrew (e.g. Genius API with API key)
- Monitor cold start times after Playwright removal
