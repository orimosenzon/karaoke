# Session 2026-04-07

## מה עשינו
1. **תיקון לייאוט מובייל** — offset controls עברו מעמודה בצד לשורה אופקית מתחת לסרטון. פינה שטח משמעותי למילים.
2. **Pipeline debug logging** — הוספנו `print()` לכל שלב ב-pipeline (lrclib_plain / genius / lyrics_ovh) כדי לראות בלוגים מה קורה.
3. **Persistent cache via Railway Volume** — הוספנו תמיכה ב-`CACHE_DIR` env var. כעת ה-cache שמור ב-`/data` (Railway Volume) ולא נמחק בין deployments.
4. **גילוי**: yt-dlp נכשל עם 429 + "Sign in as bot" — YouTube חוסם. הפול קולבק ל-lrclib/Genius/ovh.

## בעיה פתוחה
- שורות ה-`Pipeline:` לא מופיעות ב-Railway logs — כנראה Python stdout buffering.
- **פתרון**: להוסיף `PYTHONUNBUFFERED=1` ב-Railway Variables ולעשות deploy.
- אחרי זה לבדוק מה בדיוק קורה כשמחפשים "The Saga Begins" (Weird Al).

## Next steps
1. הוסף `PYTHONUNBUFFERED=1` ב-Railway Variables → deploy → בדוק Pipeline logs
2. לפי הלוגים — לתקן את השלב שנכשל
3. לבדוק שירים ישראליים (קורין אלאל) עם Genius
