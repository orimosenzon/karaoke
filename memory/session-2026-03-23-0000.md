# Session 2026-03-23

## מה נעשה

### תיקון קרדיטים — MusicBrainz
- **בעיה:** כותרות YouTube דו-לשוניות כמו `ריטה - מחכה - Rita - Mehake` גרמו ל-parse שגוי → MusicBrainz מצא שיר אחר
- **תיקון:** `_parse_title_artist` מסיר סגמנטים לטיניים כשהחלק הראשי בעברית. bump `credits_version` ל-4
- **בעיה נוספת:** כותרות בפורמט "שיר - אמן" (לא "אמן - שיר") — למשל `Un ramito de violetas - Cecilia`
- **תיקון:** מנסה שני כיוונים ב-MusicBrainz ובוחר את זה עם ציון גבוה יותר

### Wiki hover popup — שיפורים ותיקונים
- **hover mode:** חלון נפתח אחרי 400ms ריחוף, נסגר כשעוזבים
- **click mode (pinned):** לחיצה מעגנת את החלון
- **cache:** תוצאות Wikipedia נשמרות ב-JS לא לטעון מחדש
- **translate fix:** `onmousedown="_pinWikiModal()"` על ה-select — מונע סגירה כשה-dropdown נפתח
- **delay:** 800ms במקום 300ms לסגירה מה-modal
- **overlay mode:** עבר מ-CSS classes ל-inline styles כדי למנוע בעיות specificity

## בעיות פתוחות
- **Wiki overlay עדיין כהה בהover mode** — לא אומת שהתיקון האחרון (inline styles) פתר את הבעיה. לבדוק בסשן הבא
- **Wikipedia disambiguation:** חיפוש "Cecilia" מחזיר דף disambiguation במקום הזמרת הספציפית — צריך לשפר את ה-Wikipedia search logic

## Next Steps
1. לאמת שה-overlay שקוף ב-hover mode אחרי הפריסה האחרונה
2. לתקן את חיפוש Wikipedia — להוסיף ניסיון עם `(cantante)` / `(singer)` כשמגיע disambiguation page
3. לבדוק קרדיטים נוספים עם כותרות דו-לשוניות
